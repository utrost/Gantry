package org.trostheide.gantry.vectorize;

/**
 * A marker interface for a vectorization result,
 * which can be a polyline, a path, etc.
 */
public interface VectorGeometry {
    // This interface is intentionally blank.
}